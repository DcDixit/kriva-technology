# KRIVA — REFERENCE ADDENDUM

**Companion to `KRIVA-CURSOR-BRIEF.md`. Keep both at the repo root.**

**Purpose:** verified reference data, so nothing is invented. When a URL, slug, metric, contact detail, or component is needed, **look it up here**. If it is not in this file, it is not verified — mark it `[TBD]` and ask. Do not guess.

**Last verified:** 9 Aug 2026, against `https://web-nine-virid-37.vercel.app/`

---

## 1. CONTENT INVENTORY

### 1.1 Case studies — 8 total

| # | Name | Slug | Status | Published metrics | Notes |
|---|---|---|---|---|---|
| 1 | FleetFlow — US trucking dispatch CRM | `/work/fleetflow-dispatch` | `[Certain]` | **−32% manual handle time · 11 min avg exception response · 99.4% console uptime** | The **only** case study with published metrics. Proof link for the Trucking market. |
| 2 | PayrollPro — B2B SaaS onboarding | `/work/payroll-pro-saas` | `[Certain]` | **None** | Proof link for the SaaS market. An article cites "+18% activation" for a payroll client — unsourced, see §5. |
| 3 | FinanceSync — QuickBooks & Xero reconciliation hub | `/work/finance-sync-hub` | `[Certain]` | **None** | Proof link for the Accounting market. |
| 4 | BrandLift — e-commerce rebrand & launch | `/work/brandlift-ecommerce` | `[Certain]` | **None** | Verified via homepage testimonial link. |
| 5 | HealthTrack — patient mobile | `[TBD — do not invent]` | `[TBD]` | **None** | Homepage references "Patient mobile app" with **no link**. Slug unconfirmed. |
| 6 | CRMPulse — sales dashboard | `[TBD — do not invent]` | `[TBD]` | **None** | Listed in project inventory; slug never observed live. |
| 7 | SupportAI — ticket automation | `[TBD — do not invent]` | `[TBD]` | **None** | Slug never observed live. |
| 8 | LocalServe — marketplace MVP | `[TBD — do not invent]` | `[TBD]` | **None** | Slug never observed live. Matches the no-code article's unnamed "local services marketplace". |

> **METRICS RULE — absolute.** Only FleetFlow has published metrics. **Never attach a metric to any other case study**, and never carry FleetFlow's numbers to another page as if generic. Until a slug is confirmed, link to `/work`.

### 1.2 Services — 17 total, all slugs `[Certain]`

| Service (canonical label) | Slug | Family (recorded, not yet applied) |
|---|---|---|
| Product Design | `/services/product-design` | Product Interfaces |
| UI/UX Design | `/services/ui-ux-design` | Product Interfaces |
| UX Research | `/services/ux-research` | Product Interfaces |
| Wireframing & Prototyping | `/services/wireframing-prototyping` | Product Interfaces |
| Design Systems | `/services/design-systems` | Product Interfaces |
| Mobile App Design | `/services/mobile-applications` | Product Interfaces |
| Web Application Design | `/services/web-application-design` | Product Interfaces |
| SaaS Product Design | `/services/saas-platforms` | Product Interfaces |
| Dashboard Design | `/services/dashboard-design` | Product Interfaces |
| **CRM Design** | `/services/crm-development` | Product Interfaces |
| Web Design & Development | `/services/web-development` | Build & Integration |
| Integrations & APIs | `/services/api-integrations` | Build & Integration |
| AI-Assisted Product Development | `/services/ai-assisted-development` | AI & Automation |
| No-Code / Low-Code Solutions | `/services/no-code-low-code` | AI & Automation |
| Automation Workflows | `/services/automation-systems` | AI & Automation |
| Branding & Graphic Design | `/services/branding` | Brand |
| Logo Design | `/services/logo-design` | Brand |

**Naming collision — `crm-development` has three live labels:** "CRM & Dashboard Design" (`/services`), "CRM Design" (dropdown), "CRM & Dashboards" (footer). Canonical is **"CRM Design"** — the first collides with the separate `dashboard-design` service. Link labels can change now; **the service page's own H1 and `<title>` wait for Search Console.**

**Five services claimed by no market** `[Certain]`: `ux-research` · `wireframing-prototyping` · `web-application-design` · `branding` · `logo-design`.
Not orphans: `no-code-low-code` and `web-development` are both linked from insight articles.

### 1.3 Solutions — 4 total, all `[Certain]`

| Solution | Slug | Framing label | Proof link |
|---|---|---|---|
| Trucking & Logistics Solutions | `/solutions/trucking-logistics` | Trucking & logistics | FleetFlow |
| SaaS Product Solutions | `/solutions/saas` | UK & US SaaS | PayrollPro |
| QuickBooks & Xero Integrations | `/solutions/accounting-integrations` | Finance & operations | FinanceSync |
| Car Transportation | `/solutions/car-transportation` | Auto transport | **None — `[TBD]`, highest-priority content gap** |

Each solution page hides **three further capabilities** behind "+3 more". Not extractable — `[TBD]`.

### 1.4 Insights — 7 articles, all `[Certain]`

| Published (ISO) | Slug | Category | Read time |
|---|---|---|---|
| 2026-05-22 | `/insights/saas-mvp-uk-guide` | SaaS Growth | 9 min |
| 2026-05-20 | `/insights/trucking-dispatch-crm-guide` | Trucking & Logistics | 10 min |
| 2026-01-15 | `/insights/ai-in-product-design-2026` | AI & Design | 8 min |
| 2025-12-08 | `/insights/saas-onboarding-patterns` | Product | 6 min |
| 2025-11-20 | `/insights/no-code-vs-custom-mvp` | Strategy | 7 min |
| 2025-10-12 | `/insights/crm-dashboard-ux-patterns` | Product | 6 min |
| 2025-09-05 | `/insights/choosing-a-digital-agency` | Strategy | 5 min |

Order is **newest first**. The live index renders these out of order — that is a bug, not a preference. Category counts: Product 2 · Strategy 2 · SaaS Growth 1 · Trucking & Logistics 1 · AI & Design 1.

### 1.5 Top-level paths — all `[Certain]`

`/` · `/solutions` · `/services` · `/work` · `/about` · `/process` · `/insights` · `/contact` · `/faq` · `/privacy` · `/terms` · `/technologies`

**Anchors:** `/contact#book` (fit call) · `/contact#brief` (project brief). Both exist and must be preserved.

### 1.6 External links — `[Certain]`

| Target | URL |
|---|---|
| WhatsApp | `https://wa.me/919724454455?text=Hi%20KRIVA%2C%20I'd%20like%20to%20discuss%20a%20project.` |
| LinkedIn | `https://www.linkedin.com/company/kriva-technologies` |
| Instagram | `https://www.instagram.com/krivatechnologies` |
| X | `https://x.com/krivatechnologies` |
| Dribbble | `https://dribbble.com/krivatechnologies` |
| Google reviews | `https://g.page/r/kriva-technologies/review` — **`[TBD]` verification, see §5** |

---

## 2. CANONICAL CONTACT FACTS

All `[Certain]`. **Do not reformat, abbreviate, or replace** unless a later verified source supersedes them.

| Fact | Exact value | Where used |
|---|---|---|
| Email | `hello@krivatechnologies.com` | `mailto:` links · footer · `/contact` · `/privacy` §8 · `/terms` §9 |
| Phone | `+91 97244 54455` | Display text. `tel:` href must be `tel:+919724454455` (no spaces) |
| WhatsApp | `https://wa.me/919724454455` | Floating action · mobile sheet · `/contact`. Include the prefilled `?text=` param from §1.6 |
| Address | `511 - I The Address, Ahmedabad, Gujarat 380060, IN` | `/about` at-a-glance · JSON-LD `PostalAddress` · **required addition to `/privacy`, see §9** |
| Location line | `Ahmedabad, India · Remote-first · Global clients` | Footer · mobile sheet |
| Legal entity | `KRIVA Technologies` | Legal pages · JSON-LD `Organization` |
| Founded | `2025` | JSON-LD `foundingDate`. Displayed as **"Studio since 2025"** |
| Founder | `Dixit Panchal, Founder & Design Lead` | The **only** named person on the entire site |

**Response promise — canonical:** "Reply within one business day." Never "24 hours".
**CTA — canonical:** "Book a 20-minute fit call." Never "discovery call".

### JSON-LD address block — copy verbatim

```json
{"@type":"PostalAddress","streetAddress":"511 - I The Address","addressLocality":"Ahmedabad","addressRegion":"Gujarat","postalCode":"380060","addressCountry":"IN"}
```

---

## 3. CANONICAL COMPONENT OWNERSHIP MAP

**Never recreate a shared component from the prose description.** Open the source file and copy it.

| Component | Source of truth | Status | Notes |
|---|---|---|---|
| Header + market dropdown `.nav` `.mm` | `kriva-solutions-index.html` | `[Certain]` | Newest version. All four markets. Needs back-porting to every other file — sweep item 1. |
| Mobile sheet `.sheet` | `kriva-solutions-index.html` | `[Certain]` | Includes markets nested under Solutions via `a.sub`. Escape + scroll lock. |
| Footer | `kriva-solutions-index.html` | `[Certain]` | New generation: Get in touch / Trucking & logistics / SaaS / Company. |
| `.btn` (+ `.ghost .sm .on-dark`) | `kriva-solutions-index.html` | `[Certain]` | Identical in every rebuilt file. |
| `.cta-band` | `kriva-solutions-index.html` | `[Certain]` | Dark band. Slim link-row variant `.slim` on legal pages only. |
| `.slot` | `kriva-about.html` | `[Certain]` | Labelled image placeholder. Always states filename + dimensions + intended content. |
| `.flag` / `.flag.tbd` / `.flag.on-ink` | `kriva-about.html` | `[Certain]` | The universal gap marker. |
| Breadcrumbs `.crumbs` | `kriva-about.html` | `[Certain]` | Always `<nav aria-label="Breadcrumb">` + `<ol>` + `aria-current="page"`. |
| Reveal primitives `[data-r]` `[data-s]` `.mask` | Any rebuilt file | `[Certain]` | Identical everywhere. |
| Legal contents nav `.toc` | `kriva-privacy.html` | `[Certain]` | Sticky, active-section tracking, permanent `#sN` anchors. |
| Legal clause block `.cl` | `kriva-privacy.html` | `[Certain]` | Numbered heading + hover permalink. |
| Review layer `[data-review]` | `kriva-privacy.html` | `[Certain]` | Legal pages only. Kill switch: `body.review-off`. |
| Reading measure | `kriva-insight-article.html` `.prose` | `[Certain]` | 72ch for long-form prose. 62ch for `.body` / `.lede`. |
| Reading progress + sticky back-link | `kriva-insight-article.html` | `[Certain]` | Progress measured across the `<article>` element, not the page. |
| **Card components** | — | **`[TBD — choose canonical source before back-porting]`** | Five divergent variants exist: `.post` (insights ledger), `.sol` (solutions), `.ind` (about industries), `.mcard` (article related), `.prac` (about practices). They serve different content shapes; unify only if a real duplication is proven. |
| Scroll machines | Per page — see brief §5 | `[Certain]` | **Never shared. Never reused across pages.** |

---

## 4. KNOWN LIVE-SITE BUGS — DO NOT REPRODUCE

### 4.1 Implementation / content bugs — fix, never migrate

| # | Bug | Where | Conf. |
|---|---|---|---|
| B1 | Raw lowercase slug in H2 copy: `What we deliver for saas product solutions`, `Ready to discuss your quickbooks & xero integrations project?` | Solution pages | `[Certain]` |
| B2 | Duplicate CTA blocks stacked at page bottom | Live `/solutions/trucking-logistics` | `[Certain]` |
| B3 | Testimonial attributed to **FleetRoute Logistics**; the case study is published as **FleetFlow** | Homepage | `[Certain]` |
| B4 | `/insights` renders articles out of date order (Jan 26 → Dec 25 → May 26 → Nov 25 → May 26 → Oct 25 → Sep 25) | `/insights` | `[Certain]` |
| B5 | Run-together sentence: `…do not use the site.KRIVA Technologies…` | `/terms` §1 | `[Certain]` — already fixed, whitespace only |
| B6 | `Framer` listed twice on the same page — under Design *and* No-Code | `/process` | `[Certain]` |
| B7 | `GitHub` categorised under "AI" | Homepage stack | `[Certain]` |
| B8 | "Design systems" and "iOS & Android UI" listed as tools; they are capabilities | `/process` | `[Certain]` |
| B9 | Homepage presents **six** solutions — four real + two services dressed as solutions ("CRM & Workflow Automation", "AI Productivity & Automation", both linking to `/services/` URLs) | `/` | `[Certain]` |
| B10 | Same page states both "Replies in 24 hours" (body) and "one business day" (footer) | 10+ pages | `[Certain]` |
| B11 | Three fictional bylines: "KRIVA Product Team", "KRIVA Strategy Team", "KRIVA Logistics Product Team" — none appears in `/about`'s four practices | All 7 articles | `[Certain]` |

### 4.2 Genuine published content — migrate verbatim

- All legal clause text (`/privacy`, `/terms`)
- All 7 article bodies
- The founder letter on `/about`
- The five phase descriptions on `/process` (both naming systems)
- All solution descriptions and capability bullets
- **The homepage testimonials and metrics** — see §5

**The distinction that matters:** B1–B11 are defects in how content was implemented or organised. §4.2 is content someone wrote and published. Never delete §4.2 material to "clean up"; flag it instead.

---

## 5. HOMEPAGE ACCURACY CORRECTION

> **CANONICAL NOTE — supersedes any earlier statement.**
> **The homepage testimonials and metrics are live-faithful published content.** They may be strategically stale or require verification, but they were **not fabricated during this rebuild**. Do not delete them merely because they predate the current content rules.

An early project note wrongly described them as invented. That was corrected mid-project. The four testimonials are live verbatim:

| Name | Role · Company | Linked case |
|---|---|---|
| Ravi Mehta | Head of Product · FlowLedger (UK B2B SaaS) | `/work/payroll-pro-saas` |
| Marcus Cole | VP Operations · FleetRoute Logistics (US) | `/work/fleetflow-dispatch` |
| Anita Desai | Founder · Meridian D2C | `/work/brandlift-ecommerce` |
| Tom Ashworth | CTO · CarePath Health | *(no link)* |

Footnote, also live: *"Some feedback was originally shared under our previous name, Northline Digital."*

FleetFlow's three metrics are live-published. **`99.4% uptime` was once wrongly flagged as invented — that flag was withdrawn. It is real.**

### Separate verification items — track independently, do not bundle

| Item | Status | Question |
|---|---|---|
| **Google 5.0 · 8 reviews** | `[TBD]` | `/about` carries the line "without fabricated review scores", which repudiates the homepage claim. **Ask: was the reviews block deliberately removed, or is the claim still verified and intended to remain live?** If deliberately removed → delete from homepage. |
| **15+ senior team** | `[TBD]` | Sits against "small team, founder-led" on the same page and against `/about`'s no-headcount position. Verify or remove. |
| **40+ projects · 9+ years** | `[TBD]` | Against "Founded 2025". `/about` handles this correctly by attributing to **founder experience**. Apply the same framing or verify. |
| **+18% activation** (onboarding article) | `[TBD]` | Matches the FlowLedger testimonial and PayrollPro — but PayrollPro publishes no metrics. Needs source and permission, or removal. |

---

## 6. STANDARD PAGE-SECTION PATTERN

**Default, not a checklist:**

> Hero + machine → scroll story → domain section → capabilities → proof → process → related work → related services → FAQ → CTA

**Omit any section when:**
- no verified content exists for it,
- it duplicates a canonical page (`/process` owns delivery detail; `/services` owns capability grouping; `/technologies` owns the stack),
- it does not serve the page's buyer question.

**Never invent content to satisfy the pattern.** Pages already built that deliberately depart from it — and were right to:

| Page | Departure |
|---|---|
| `/solutions` | No machine, no scroll story. It is a hub; its job is to get people off it quickly. |
| `/insights` | No machine. Filter + true date sort is a fix, not a machine. |
| `/insight-article` | No machine. Progress bar and sticky back-link only. |
| `/privacy`, `/terms` | No machine, no proof, no capabilities. Legal documents. |
| `/about` | No FAQ, no related services. |

**Process presentation must vary per page** — vertical rail / horizontal stepper / pipeline with gates / connected-dot arc — so pages don't read as clones.

---

## 7. VALIDATION CHECKLIST

Run after **every** build and after **every** shared-component change.

### 7.1 Available tooling — inspect before assuming

```bash
which python3 node npx grep
python3 -c "import html.parser; print('ok')"
```

This project has **no build step and no dependencies**. Prefer stdlib Python and `grep`. Do not add a validation toolchain that is not already present; if you want `npx` linters, ask first.

### 7.2 Automated structural checks — save as `validate.py` at the repo root

```python
#!/usr/bin/env python3
"""KRIVA structural validator. Stdlib only. Usage: python3 validate.py [files...]"""
import sys, re, glob
from html.parser import HTMLParser

VOID = {'area','base','br','col','embed','hr','img','input','link','meta','source','track','wbr'}
KNOWN_PATHS = {
 '/','/solutions','/services','/work','/about','/process','/insights','/contact',
 '/faq','/privacy','/terms','/technologies','/contact#book','/contact#brief',
 '/work/fleetflow-dispatch','/work/payroll-pro-saas','/work/finance-sync-hub','/work/brandlift-ecommerce',
 '/solutions/trucking-logistics','/solutions/saas','/solutions/accounting-integrations','/solutions/car-transportation',
 '/insights/saas-mvp-uk-guide','/insights/trucking-dispatch-crm-guide','/insights/ai-in-product-design-2026',
 '/insights/saas-onboarding-patterns','/insights/no-code-vs-custom-mvp','/insights/crm-dashboard-ux-patterns',
 '/insights/choosing-a-digital-agency',
}
# Verified publication dates — inventory §1.4
KNOWN_DATES = {'2026-05-22','2026-05-20','2026-01-15','2025-12-08','2025-11-20','2025-10-12','2025-09-05'}

KNOWN_PATHS |= {'/services/'+s for s in (
 'product-design','ui-ux-design','ux-research','wireframing-prototyping','design-systems',
 'mobile-applications','web-application-design','saas-platforms','dashboard-design','crm-development',
 'branding','logo-design','web-development','api-integrations','ai-assisted-development',
 'no-code-low-code','automation-systems')}

class V(HTMLParser):
    def __init__(self):
        super().__init__(convert_charrefs=True)
        self.stack, self.errors, self.ids = [], [], {}
        self.headings, self.aria = [], []
    def handle_starttag(self, tag, attrs):
        d = dict(attrs)
        if 'id' in d:
            self.ids[d['id']] = self.ids.get(d['id'], 0) + 1
        if re.fullmatch(r'h[1-6]', tag):
            self.headings.append(int(tag[1]))
        if 'aria-controls' in d:
            self.aria.append(d['aria-controls'])
        if tag not in VOID:
            self.stack.append((tag, self.getpos()))
    def handle_endtag(self, tag):
        if tag in VOID: return
        if self.stack and self.stack[-1][0] == tag:
            self.stack.pop()
        else:
            self.errors.append(f"unexpected </{tag}> at line {self.getpos()[0]}")

def strip_code(t):
    """Remove <style>, <script> and comments. CSS/JS are not page content;
    scanning them produces false positives (keyframe percentages, JS-built anchors)."""
    return re.sub(r'<style.*?</style>|<script.*?</script>|<!--.*?-->', ' ', t, flags=re.S)

def check(path):
    src = open(path, encoding='utf-8').read()
    clean = strip_code(src)          # markup only
    p = V(); p.feed(src)
    fails = []

    # 1 tag balance + duplicate ids
    if p.stack:  fails.append(f"unclosed tags: {[t for t,_ in p.stack][:5]}")
    fails += p.errors[:5]
    dupes = [k for k, v in p.ids.items() if v > 1]
    if dupes: fails.append(f"duplicate ids: {dupes}")

    # 2 internal anchors resolve
    for a in set(re.findall(r'href="#([^"]+)"', clean)):
        if a and a not in p.ids: fails.append(f"broken anchor #{a}")

    # 3 aria-controls targets exist
    for t in set(p.aria):
        if t not in p.ids: fails.append(f"aria-controls -> missing id '{t}'")

    # 4 placeholder / unverified links
    if re.search(r'href="#"', clean): fails.append('placeholder href="#" present')
    for h in set(re.findall(r'href="(/[^"#?]*)', clean)):
        if h not in KNOWN_PATHS: fails.append(f"UNVERIFIED path: {h}")

    # 5 exactly one h1, no skipped heading levels
    if p.headings.count(1) != 1: fails.append(f"h1 count = {p.headings.count(1)}")
    for a, b in zip(p.headings, p.headings[1:]):
        if b > a + 1: fails.append(f"heading jump h{a} -> h{b}")

    # 6 accessibility + motion baseline
    for needed, label in [
        ('prefers-reduced-motion', 'reduced-motion block'),
        (':focus-visible', 'focus-visible styles'),
        ('class="skip"', 'skip link'),
        ('<main', '<main> landmark'),
    ]:
        if needed not in src: fails.append(f"missing {label}")

    # 7 legal pages only
    if 'privacy' in path or 'terms' in path:
        if '@media print' not in src: fails.append('missing print stylesheet')
        if 'data-review' in src and 'body.review-off [data-review]' not in src:
            fails.append('review layer present without kill switch')

    # 8 invented-content tripwires — scan PROSE only.
    # Strip <style>, <script>, HTML comments and the [data-review] staging layer,
    # otherwise CSS percentages and review notes quoting banned strings drown the signal.
    prose = re.sub(r'<[^>]*\bdata-review\b.*?</div>', ' ', clean, flags=re.S)
    prose = re.sub(r'\sstyle="[^"]*"', ' ', prose)  # inline CSS is not prose
    for pat, msg in [
        (r'\b(?:19|20)\d{2}-\d{2}-\d{2}\b', 'DATE'),
        (r'\b\d{1,3}(?:\.\d)?\s?%', 'percentage — only FleetFlow may carry metrics (§1.1)'),
        (r'discovery call', 'banned CTA wording — use "20-minute fit call"'),
        (r'24 hours', 'banned response promise — use "one business day"'),
        (r'15\+ senior|Google 5\.0|8 reviews', 'unverified claim — see §5'),
    ]:
        for m in set(re.findall(pat, prose, re.I)):
            if msg == 'DATE':
                if m not in KNOWN_DATES:
                    fails.append(f"UNVERIFIED date (not in inventory §1.4): '{m}'")
            else:
                fails.append(f"{msg}: '{m}'")

    print(('FAIL ' if fails else 'PASS ') + path)
    for f in dict.fromkeys(fails): print('   -', f)
    return not fails

files = sys.argv[1:] or sorted(glob.glob('kriva-*.html'))
sys.exit(0 if all([check(f) for f in files]) else 1)
```

Run: `python3 validate.py` — or `python3 validate.py kriva-about.html` for one file.

> Tripwires 8 are intentionally noisy. Every hit needs a human decision: **verified against §1/§5, or removed.** Suppressing a warning without checking defeats the purpose.

### 7.3 Manual checks — automation cannot cover these

| # | Check | How |
|---|---|---|
| M1 | Keyboard: full page reachable, logical order, no traps | Tab through start to finish |
| M2 | Disclosures: Escape closes, outside-click closes, focus returns to trigger | Nav dropdown, mobile sheet, accordions |
| M3 | Focus visible on every interactive element, including on dark | Tab with the mouse untouched |
| M4 | `aria-expanded` flips on toggle; `aria-current="page"` on the active nav item; `aria-live` fires on state change | DevTools accessibility pane |
| M5 | Reduced motion: enable OS setting → no transitions, **no delays**, all content visible | System preferences |
| M6 | Responsive: 320 / 375 / 768 / 1024 / 1440 — no horizontal overflow, sticky panels pin at `top:62px` | DevTools device toolbar |
| M7 | Every `.slot` states filename + dimensions + intended content | Visual scan |
| M8 | Every `.flag.tbd` names a real open question, not decoration | Visual scan |
| M9 | **No `data-review` content is visible in a production build** | Add `review-off`, reload, confirm nothing amber remains |
| M10 | Header and footer byte-identical to the canonical source in §3 | `diff` the extracted blocks |
| M11 | Print legal pages: clauses legible, chrome gone, numbering intact | Print preview |
| M12 | **No invented metrics, names, testimonials, URLs, dates or contact facts** | Cross-check every one against §1, §2, §5 |

M12 is the one that matters most. Everything else is recoverable.

---

## 8. EVIDENCE-STATUS CONVENTION

| Tag | Meaning |
|---|---|
| `[Certain]` | Verified from a live or published source |
| `[Likely]` | Strong inference, not yet verified |
| `[TBD]` | Requires source, client, counsel, or Search Console confirmation |
| `[BLOCKED]` | Cannot proceed honestly without evidence — stop and ask |

> **Cursor must never convert `[Likely]`, `[TBD]`, or `[BLOCKED]` material into production fact without a verified source.**

Downgrading a tag requires new evidence, not new confidence. If a tag is removed, the commit or build note must say what source justified it. When in doubt, ship the visible `.flag.tbd` — a marked gap is a feature of this project, not a defect.

---

## 9. CURRENT BLOCKERS

| # | Workstream | Blocked on | Status |
|---|---|---|---|
| 1 | `/technologies` | Live page content pasted in — the URL cannot be fetched | `[BLOCKED]` |
| 2 | `/faq` | 13 collapsed answers + 4 solution-page FAQ blocks, verbatim (`KRIVA-FAQ-ANSWER-CAPTURE.md`) | `[BLOCKED]` |
| 3 | Legal processor / cookie inventory | Actual tooling evidence — checklist A1–A8 in the sweep backlog | `[BLOCKED]` |
| 4 | Service-page merge + service template | Search Console data on the 17 service URLs | `[BLOCKED]` |
| 5 | **Screenshots and visual assets** | Client supply | `[BLOCKED]` — **flagged since session one and still open. Every page has labelled empty slots. The site cannot ship visually complete without these; it is the single highest-value input on the project.** |

### Two blockers unlock other pages, not just their own

- **FAQ Q9** — "What do you need from us to get started?" fills the five `[TBD]` *You provide* cells on `/process`.
- **FAQ Q14** — "Which time zones do you support?" resolves the `[TBD]` hour bands on the `/about` coverage clock.

### Cheapest high-value actions, in cost order

1. **Identify the analytics tool** — `/privacy` says analytics are used; no tool is named anywhere on the site. DevTools → Network + Application → Cookies, clean profile. ~90 seconds. Top legal blocker.
2. **Confirm whether HubSpot is KRIVA's own CRM** — if it holds inquiry data it is an undisclosed processor. A fact, not a legal question.
3. **Capture the FAQ answers** — one browser session, unlocks two page gaps.

---

**Required companion file: `KRIVA-CURSOR-BRIEF.md`**
